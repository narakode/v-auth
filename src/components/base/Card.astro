---
const {
  tag: Tag = 'div',
  class: classList = '',
  hasTab = false,
  ...attrs
} = Astro.props;
---

<Tag
  {...attrs}
  class={`bg-white border border-slate-200 p-5 border-2 ${classList} ${hasTab ? 'border-t-0 rounded-b-lg' : 'rounded-lg'}`}
>
  <slot />
</Tag>
