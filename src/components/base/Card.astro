---
const {
  tag: Tag = 'div',
  class: classList = '',
  customBorder = false,
  ...attrs
} = Astro.props;
---

<Tag
  {...attrs}
  class={`bg-white p-5 ${classList} ${customBorder ? '' : 'border-2 border-slate-200 rounded-lg'}`}
>
  <slot />
</Tag>
